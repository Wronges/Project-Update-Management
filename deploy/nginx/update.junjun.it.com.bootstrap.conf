server {
    listen 80;
    listen [::]:80;
    server_name update.junjun.it.com;

    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    location / {
        return 302 https://$host$request_uri;
    }
}
